{
  "name": "@goldstack/template-repository-bucket",
  "version": "0.1.0",
  "license": "",
  "author": "",
  "sideEffects": false,
  "main": "src/bucket.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn clean && yarn compile",
    "clean": "rimraf dist/",
    "compile": "tsc -p tsconfig.json",
    "coverage": "jest --collect-coverage --passWithNoTests --config=./jest.config.js --runInBand",
    "deploy": "template deploy $@",
    "infra": "template infra",
    "prepublishOnly": "yarn run build",
    "template": "template",
    "test": "jest --passWithNoTests --config=./jest.config.js --runInBand"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1096.0",
    "@goldstack/template-s3": "0.5.87",
    "@goldstack/utils-sh": "0.5.43",
    "@smithy/smithy-client": "^4.14.15",
    "@smithy/types": "^4.16.1"
  },
  "devDependencies": {
    "@goldstack/template-s3-cli": "0.6.111",
    "@swc/core": "^1.15.46",
    "@swc/jest": "^0.2.39",
    "@types/jest": "^30.0.0",
    "@types/node": "^25.0.3",
    "concurrently": "^7.3.0",
    "jest": "^30.4.2",
    "rimraf": "^6.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
