{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$ref": "#/definitions/ThisDeploymentConfiguration",
  "definitions": {
    "ThisDeploymentConfiguration": {
      "type": "object",
      "properties": {
        "backupVaultName": {
          "type": "string"
        },
        "allowedAccountIds": {
          "$ref": "#/definitions/AllowedAccountIds"
        },
        "sourceAccountIds": {
          "$ref": "#/definitions/SourceAccountIds"
        },
        "sourceRoleArns": {
          "$ref": "#/definitions/SourceRoleArns"
        }
      },
      "required": [
        "backupVaultName",
        "allowedAccountIds"
      ]
    },
    "AllowedAccountIds": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of AWS account IDs allowed to copy backups to this central vault.",
      "title": "Allowed Account IDs"
    },
    "SourceAccountIds": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of AWS account IDs that are sources for cross-account backup copies.",
      "title": "Source Account IDs"
    },
    "SourceRoleArns": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of IAM role ARNs from source accounts allowed to copy backups to this vault.",
      "title": "Source Role ARNs"
    }
  }
}