{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$ref": "#/definitions/AWSDockerImagePackageConfiguration",
  "definitions": {
    "AWSDockerImagePackageConfiguration": {
      "type": "object",
      "properties": {
        "imageTag": {
          "type": "string",
          "description": "Defines the tag for identifying the docker image.",
          "title": "Image Tag"
        }
      },
      "required": ["imageTag"],
      "description": "Configures a docker image deployed to AWS.",
      "title": "AWS Docker Image Configuration"
    }
  }
}
