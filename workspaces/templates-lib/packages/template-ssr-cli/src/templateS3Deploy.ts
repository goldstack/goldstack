import { SSRConfiguration } from '@goldstack/template-ssr';

export const deployCli = async (
  config: SSRConfiguration,
  args: string[]
): Promise<void> => {
  throw new Error('Deploy not supported yet.');
};
