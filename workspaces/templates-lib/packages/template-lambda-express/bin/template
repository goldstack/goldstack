#!/usr/bin/env node
/* eslint-disable @typescript-eslint/no-var-requires */

require('./../dist/src/templateLambdaExpress')
  .run(process.argv)
  .catch((e) => {
    console.log(e);
    process.exit(1);
  });
