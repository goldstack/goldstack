import express from 'express';
import type { Server } from 'http';
export declare const app: express.Application;
export declare const start: (port: number) => Promise<Server>;
//# sourceMappingURL=server.d.ts.map
