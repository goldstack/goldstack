#!/usr/bin/env node
require('source-map-support').install();

require('../dist/utilsGoldstackDocsCliRun.js')
  .run()
  .catch((e) => {
    console.error(e);
  })
  .then(() => {
    // all good
  });
